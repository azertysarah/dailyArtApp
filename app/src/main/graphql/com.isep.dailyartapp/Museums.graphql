query Museums {
    taxonomyTermQuery(
        filter: {conditions: [{field: "vid", value: "musee"}]}
        limit: 20
    ) {
        entities {
            entityUuid
            entityLabel
        }
    }
}