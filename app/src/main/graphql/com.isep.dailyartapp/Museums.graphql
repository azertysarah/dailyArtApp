query Museums {
    taxonomyTermQuery(
        filter: {conditions: [{field: "vid", value: "musee"}]}
        limit: 20
    ) {
        count
        entities {
            entityUuid
            entityLabel
        }
    }
}